import React from 'react';
import { Check, X } from 'lucide-react';
export const ProductDetails = () => {
  const included = ['Yield: Makes 20-25 full spray bottles (750ml each)', 'Supply Duration: 3-4 months for average household cleaning', 'Key Technology: Surface-Activated Chemistry with low-volatility PGE solvents', 'Chelating System: GLDA + MGDA + Citric Acid (works in any water hardness)'];
  const notIncluded = ['No synthetic fragrances', 'No essential oils', 'No phthalates', 'No parabens', 'No volatile ethylene glycol ethers', 'No hormone-disrupting chemicals'];
  const bonuses = ['Complete dilution guide + ingredient transparency documentation with CAS numbers'];
  return <section className="py-12 container mx-auto px-4">
      
    </section>;
};